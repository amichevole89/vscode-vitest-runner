{
  "name": "vscode-vitest-runner-optimized",
  "displayName": "Vitest Runner Optimized",
  "description": "Run, debug, or stress-test individual Vitest cases from your editor. Lightweight and zero-config.",
  "version": "0.2.1",
  "publisher": "amichevole",
  "license": "MIT",
  "icon": "logo.png",
  "repository": "git@github.com:amichevole89/vscode-vitest-runner.git",
  "author": "amichevole89 <postino.amichevole@gmail.com>",
  "engines": {
    "vscode": "^1.65.0"
  },
  "categories": [
    "Testing",
    "Other"
  ],
  "keywords": [
    "vitest",
    "test",
    "runner",
    "debug",
    "flaky",
    "testing"
  ],
  "main": "dist/index.js",
  "activationEvents": [
    "onLanguage:typescript",
    "onLanguage:javascript",
    "onLanguage:typescriptreact",
    "onLanguage:javascriptreact"
  ],
  "contributes": {
    "configuration": {
      "title": "Vitest Runner Optimized",
      "properties": {
        "vitestRunner.loopCount": {
          "type": "number",
          "default": 20,
          "minimum": 1,
          "description": "How many times the Run(×N) buttons will execute a test."
        },
        "vitestRunner.extraArgs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Extra args appended to vitest commands (e.g. [\"--reporter\",\"verbose\"])."
        },
        "vitestRunner.forceColorEnv": {
          "type": "boolean",
          "default": true,
          "description": "Set FORCE_COLOR=1 for vitest so ANSI colors render in the terminal."
        },
        "vitestRunner.addColorFlag": {
          "type": "boolean",
          "default": true,
          "description": "Append --color to vitest args to ensure colored output."
        }
      }
    },
    "commands": [
      {
        "command": "vitest.runTest",
        "title": "Vitest: Run Test"
      },
      {
        "command": "vitest.debugTest",
        "title": "Vitest: Debug Test"
      },
      {
        "command": "vitest.runTestLoop",
        "title": "Vitest: Run Test Multiple Times"
      },
      {
        "command": "vitest.runFile",
        "title": "Vitest: Run All Tests in File"
      }
    ]
  },
  "scripts": {
    "build": "node ./scripts/build.js",
    "package": "npm run build && npx vsce package",
    "format:check": "prettier --config ./prettier.config.json --c ./src/**/*.ts",
    "format:write": "npm run format:check -- --write"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/react": "^19.2.14",
    "@types/vscode": "^1.65.0",
    "esbuild": "^0.14.27",
    "prettier": "^2.6.0",
    "typescript": "^4.9.5",
    "vitest": "^0.7.7",
    "vsce": "^2.7.0"
  }
}
